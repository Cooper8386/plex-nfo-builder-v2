import { createRoot } from 'react-dom/client';

createRoot(document.getElementById('root')!).render(
  <main><h1>Plex NFO Builder</h1><p>The application foundation is ready. Library management arrives in later phases.</p></main>,
);
