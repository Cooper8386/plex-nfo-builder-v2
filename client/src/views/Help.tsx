import { Link } from 'react-router';
export function Help() {
  return <article className="help-view"><header className="page-heading"><div><h1>Keep metadata with your media.</h1><p>A guide to matching, building and refining your collection.</p></div></header><section className="panel"><h2>Start with one title</h2><ol><li>Set provider credentials and preferred language in <Link to="/settings">Settings</Link>.</li><li>Detect libraries, choose their metadata providers, and scan local folders.</li><li>Open a title. Auto-match uses folder IDs first; manual search lets you bind the correct record. Lock a binding to protect it from automatic matching.</li><li>Choose season posters in Artwork. Each season with local videos needs its own saved choice.</li><li>Build NFOs and follow progress in <Link to="/jobs">Jobs</Link>.</li></ol></section><section className="panel"><h2>Read the status</h2><dl><dt>No NFOs</dt><dd>No local NFO metadata found.</dd><dt>Partial</dt><dd>Some required metadata is missing. Open the status explanation for filenames and season coverage.</dd><dt>Complete</dt><dd>Local NFO coverage satisfies the scanner’s checks. Season-poster selection is measured separately.</dd><dt>Foreign NFOs</dt><dd>NFOs came from another application.</dd><dt>Mixed NFOs</dt><dd>Both this app’s NFOs and foreign NFOs are present.</dd></dl><p>All / Needs work / Complete filters NFO status. The optional Season posters filter lives inside Filters. Not started and In progress both need selection; new seasons can return a previously selected show to needs selection.</p></section><section className="panel"><h2>Refine a title</h2><p>Overrides save custom titles, sort titles, taglines and plots. Empty values use provider data. Episode thumbnails offer provider stills and an Auto choice. Build again to write saved changes to disk.</p><p>Artwork choices override automatic defaults. Seasons use manual choices only. Custom images can be uploaded or registered by URL. A secondary provider ID connects artwork and identifiers when the primary record lacks a cross-reference.</p><p>Episodes maps files to season/episode numbers or provider episodes. Rename to scheme previews exact filenames and companions. Choose Auto, Standard, Daily or Anime, review conflicts, and apply the checked subset.</p></section><section className="panel"><h2>Use destructive actions deliberately</h2><p>Danger Zone previews cleanup targets before confirmation. Media and season folders are protected by cleanup. Removing a library from the app preserves disk files; deleting sidecars removes recovery records.</p><p>Rename and custom-image deletion retain captured previews. If files, selections or other required targets change, review a fresh preview. A preview expires after 30 minutes or a worker restart. Skipped and failed operations are reported.</p><p>Automatic orphan cleanup starts disabled. Enabling it explicitly authorizes later post-build sweeps. Orphan NFOs and thumbnails are removed only when their video stem is no longer present.</p></section><section className="panel"><h2>Automate and troubleshoot</h2><p><Link to="/watcher">Watcher</Link> waits for stable media, then matches and queues builds. Review failures there, correct bindings, and retry the pipeline. The server kill switch overrides the UI toggle.</p><p>Schedules in Settings use five-field UTC cron. Run now also works for disabled schedules. A successful schedule can mean builds were submitted; check their individual results in Jobs.</p><p>Query failures show Retry. Settings saves update one field at a time and keep unsaved drafts during navigation. Secrets are never returned by the settings API; replacement secret drafts stay in browser memory.</p><p>For Plex, configure credentials and path mappings, test the connection, and manually refresh a folder. Application logs show available log output; job logs provide build details.</p></section></article>;
}
