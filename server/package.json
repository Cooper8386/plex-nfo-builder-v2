{
  "name": "server", "version": "0.12.0", "private": true, "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --root .."
  },
  "dependencies": { "shared": "workspace:*", "fastify": "^5.12.3", "@fastify/cors": "^11.0.0", "better-sqlite3": "^12.11.1", "zod": "^4.0.0" },
  "devDependencies": { "@types/better-sqlite3": "^7.6.0" }
}
